pipeline {
     agent any
     stages {
          stage("Variables setup") {
               steps {
                    echo "Setting up variables for the build"
               }
          }
     }
}
